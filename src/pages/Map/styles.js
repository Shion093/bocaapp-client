const styles = theme => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
  },
  map: {
    width: '100%',
    height: window.innerHeight - 150,
  }
});

export default styles;

